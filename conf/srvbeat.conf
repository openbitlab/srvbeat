[general]
name = srvbeat1
port = 65432
beatTimeout = 300
master = true
peers =
callEnabled = false
callAfter = 10

; optional metric thresholds: alert when a node reports a value above these
; (disk/mem are percentages, load is the 1-minute load average). Remove or
; comment out a line to disable that alert.
; disk = 90
; mem = 90
; load = 8


; telegram notifications 
[telegram]
apiToken = 
chatId = 


; twilio call notification
[twilio]
accountSid =
authToken = 
from = 
to = 